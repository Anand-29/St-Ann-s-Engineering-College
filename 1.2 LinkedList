class Node:
    def __init__(self,d):
        self.data=d
        self.next=None
class LinkedList:
    def __init__(self):
        self.head=None
    def insertend(self,data):
        newnode = Node(data)
        if(self.head==None):
            self.head=newnode
        else:
            t=self.head
            while(t.next!=None):
                t=t.next
            t.next=newnode    
    def display(self):
        t=self.head;
        while(t!=None):
            print(t.data,end=" ")
            t=t.next
ob=LinkedList()
n=int(input("Enter the number of data: "))
print("Enter",n,"data: ")
for i in range(n):
    a=int(input())
    ob.insertend(a)
print("Data's of Linked list")
ob.display()
